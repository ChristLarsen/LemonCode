import { AppLayout } from "@/layout";
import React from "react";

export const TransferPage : React.FC = () => {

    return (
        <AppLayout>
            <div>Transfer</div>
        </AppLayout>
    );

};
